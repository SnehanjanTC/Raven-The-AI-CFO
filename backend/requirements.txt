# Raven Backend Dependencies

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
cryptography>=42.0.0
python-multipart>=0.0.9

# HTTP & Utilities
httpx>=0.27.0
python-dateutil>=2.8.0

# AI Provider
# anthropic>=0.7.0       # Claude API (add when implementing AI features)

# Error Tracking & Monitoring
sentry-sdk[fastapi]>=2.0.0

# Production Server & Performance (optional but recommended)
uvloop>=0.21.0          # High-performance event loop
# asyncpg>=0.29.0       # PostgreSQL async driver (if using Postgres)
# redis>=5.0.0          # Caching
# celery>=5.3.0         # Async task queue

# Logging & Observability
python-json-logger>=2.0.0

# Database Migrations (optional)
# alembic>=1.13.0       # Database schema migrations
